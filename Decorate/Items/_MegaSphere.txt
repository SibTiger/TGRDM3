ACTOR TGRMegaSphere : MegaSphere REPLACES MegaSphere
{
    // Flags, Switches, and Properties
    // =================================
        +NoGravity
        +FloatBob
    // ------
    // ======
    States
    {
    Spawn:
        // Allow a function to be called immediately
            TNT1 A 0 NoDelay
        // Spawn the item, but raise it by z-offset value
            MEGA ABCD 6 BRIGHT A_Warp (AAPTR_DEFAULT, 0, 0, 8)
        Stop
    }
}